components:
  schemas:
    # Web user
    WebUserDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        firstName:
          type: string
        lastName:
          type: string
        emailAddress:
          type: string
        registrationDate:
          type: string
          format: date-time
